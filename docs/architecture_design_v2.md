# AI Video Generation Pipeline (Auto Content Factory) 架构设计文档 V2.0

## 1. 项目概述

本项目旨在构建一个完全自动化的“AI 内容工厂”（Auto Content Factory）。系统不仅能根据人工输入的提示词生成视频，还能通过网络爬虫自动捕捉社交平台热点，结合大语言模型（LLM）进行创意生成，最终利用多模态大模型（如 Video Gen API）完成包含统一画风、原生音效和配音的高质量短视频生成与拼接。

## 2. 核心架构演进：从被动工具到自动引擎

引入了**“热点挖掘模块”**和基于 Video Gen API 的**“多模态联合装填策略”**后，整个系统从单向的 `文本 -> 视频` 工具，升级为具备自我驱动能力的闭环生产引擎。

### 核心工作流 (Workflow V2)

1. **模块 0（自动触发与热点创意）**：定时触发爬虫抓取各大平台（微博、Twitter、抖音等）榜单，交由 LLM 筛选并结合特定画风生成视频创意大纲。
2. **模块 1（剧本拆解与分镜）**：LangChain 接管创意，扩写为详细的故事剧本，并拆解为多个独立的分镜（每个分镜控制在 5-15 秒，包含画面描述、动态指令、旁白台词）。
3. **模块 2（视觉定调与素材准备）**：根据分镜需求，调用外部 API（如 Midjourney/SD）生成角色和场景的关键帧图片，或者提取爬虫抓取的源视频/图片素材。
4. **模块 3（多模态联合生成 - 核心层）**：将模块 2 准备的素材（图片/视频）、分镜提示词、台词和音效需求进行“策略化组装”，并发调用 **Video Gen API** API，直接产出**自带口型同步配音和音效**的视频片段。
5. **模块 4（自动剪辑与合成）**：使用 MoviePy/FFmpeg 将返回的各分镜视频片段（由于原生带音频，无需二次对齐）直接首尾相连，渲染输出最终的高清视频。

## 3. 核心设计：素材组装策略引擎 (Asset Strategy Engine)

为打破“只能用 AI 生成图片作为参考”的局限，系统在模块 3 之前抽象出了一层**策略工厂（Strategy Pattern）**，用于动态决定当前分镜如何向视频模型喂入参考素材（图片、上次生成的视频、爬虫原视频等）。

### 策略 1：纯原创/风格一致性策略 (Original & Consistent Strategy)

- **适用场景**：虚构故事、科幻短片、绘本动画等从零开始的内容。
- **输入组合**：`【Midjourney 生成的关键帧图片】 + 动作提示词 + 台词文本`
- **优势**：保证长篇叙事中的人物长相和整体画风绝对一致。

### 策略 2：热点二创/缝合策略 (Trend Remix Strategy)

- **适用场景**：社会热点解读、新闻二次创作、搞笑鬼畜。
- **输入组合**：`【爬虫抓取的热搜原视频/新闻截图】 + 二创剧本台词 + 风格化重绘指令`
- **优势**：利用视频模型的 Video-to-Video 能力，对真实热点素材进行快速风格化（如转成赛博朋克风）或动作延续，极具现实相关性和爆款潜质。

### 策略 3：时间轴顺延长镜头策略 (Timeline Extension Strategy)

- **适用场景**：需要突破单次 15 秒限制，生成极度连贯的长镜头动作。
- **输入组合**：`【上一段刚刚生成的视频的最后一帧/最后3秒】 + 下一步动作指令 + 延续台词`
- **优势**：让下一段视频的起点严格贴合上一段的终点，实现无缝衔接的无限长镜头。

### 策略 4：全多模态狂暴策略 (Full Multi-modal Strategy)

- **适用场景**：终极爆款制造、极度复杂的创意缝合。
- **输入组合**：`@image (固定主角脸) + @video (爬虫抓取的搞笑动作作骨架) + @audio (特定BGM) + 文本指令`
- **优势**：充分压榨多模态模型的 12 输入位上限，将他人动作、自设主角和爆款音乐强行融合。

## 4. 技术栈选型总结

- **调度与编排**：Python 3.10+
- **LLM/Agent 框架**：LangChain / LlamaIndex (处理模块 0 和模块 1 的多步推理)
- **爬虫技术**：`requests` + `BeautifulSoup4` (轻量级) / `Playwright` (需要绕过反爬的动态页面)
- **图像生成 API**：Midjourney API (通过第三方) 或 Stable Diffusion API (SDXL)
- **音视频联合生成 API**：**ByteDance Video Gen API** (或兼容聚合平台如 VO3 AI)
- **视频后期合成**：`MoviePy` / `ffmpeg-python`
- **数据交互格式**：全流程结构化 JSON

## 5. 抽象代码架构示例 (Python)

```python
class AssetStrategy(Enum):
    STORY_MODE = "STORY_MODE"
    REMIX_MODE = "REMIX_MODE"
    CONTINUOUS_MODE = "CONTINUOUS_MODE"

class AssetBuilder:
    def __init__(self, strategy: AssetStrategy):
        self.strategy = strategy

    def build_payload(self, scene_data: dict, previous_video=None, crawled_assets=None):
        payload = {
            "prompt": scene_data["action_text"],
            "audio_prompt": scene_data["voiceover"],
            "references": []
        }

        if self.strategy == AssetStrategy.STORY_MODE:
            payload["references"].append({"type": "image", "url": scene_data["generated_image_url"]})

        elif self.strategy == AssetStrategy.REMIX_MODE:
            payload["references"].append({"type": "video", "url": crawled_assets["hot_topic_video_url"]})
            payload["prompt"] = f"Maintain action but change style. {scene_data['action_text']}"

        elif self.strategy == AssetStrategy.CONTINUOUS_MODE:
            if previous_video:
                payload["references"].append({"type": "video", "url": previous_video, "role": "first_frame"})

        return payload
```

## 6. 后续开发计划

1. 初始化项目标准目录结构。
2. 开发模块 0：编写爬虫脚本并接入 LLM，完成“热点获取 -> 创意输出”的测试。
3. 构建 `AssetBuilder` 策略工厂核心类。
4. 接入相关图像与视频生成 API，跑通首个测试用例。

## 7. 团队职责分配方案 (5人编队)

为了支撑以上 AI 内容工厂闭环自动生成架构，建议的团队职责划分如下：

### 1. 架构师与 AI 引擎核心（wuke）

- **抽象职责**：系统整体架构设计、技术选型把控、核心难点攻坚、团队研发进度统筹。
- **具体项目职责**：
  - 维护与迭代本《架构设计 V2.0》以及衍生策略（如《人工智能视频生成策略.md》）。
  - 开发 `AssetBuilder` 策略工厂核心类，实现 4 大底层素材组装策略（故事化、热点二创缝合、长镜头顺延、全模态融合）。
  - 负责模块 3（多模态联合生成层）的落地，对接和优化 Video Gen API 以及 LLM 推理调度（LangChain）。
  - 编排视频渲染和任务引擎的流转（如基于 Celery 的分布式处理）和视频处理（MoviePy合成）。

### 2. 爬虫与数据工程师 (huyuxuan)

- **抽象职责**：多源高并发数据采集、数据清洗与结构化入库、反爬监控与应对。
- **具体项目职责**：
  - 负责模块 0（自动触发与热点创意）的核心外部数据入口，编写和维护对应爬虫（基于 `requests`/`Playwright`）。
  - 开发针对全网社交媒体（微博、Twitter、小红书、抖音等）榜单、热点文章和短视频源文件的自动化抓取微服务。
  - 构建标准化本地素材库及清洗管道，剔除无效数据，为 LLM 的创意生成提供高质量的高热度源数据和素材（视频/截图）。

### 3. 后端业务开发工程师 (luyi)

- **抽象职责**：支撑业务运转的标准服务器端开发、业务逻辑解耦、数据库结构设计、文件资产存储管理。
- **具体项目职责**：
  - 各类基础支撑服务：用户中心（注册/权限/配额）、计费与使用量记录。
  - 多媒体云端文件生命周期管理：统筹爬虫抓取回来的海量源素材、各类 API 产生的关键帧图片、以及最终拼接完成的高清短视频，统一落库入盘（OSS/S3 与数据库映射）。
  - 封装核心层提供的业务接口供前端调用，提供安全的高吞吐量 Web API（如视频排队进度查询、工作流下发）。

### 4. 前端开发与核心交互工程师 (shaddy)

- **抽象职责**：复杂客户端全栈工程化架构开发、核心应用状态管理、高度复杂工具类交互实现。
- **具体项目职责**：
  - 牵头开发 AI 内容工厂的 Web 交互端。特别是与用户强交互的“视频编排编辑器台”（如用户干预配置不同的策略路线）。
  - 复杂数据与状态流对接：负责接收模块 1 拆解出的分镜剧本数据，进行前端可视化呈现和用户二次编辑修改。
  - 建立客户端与后端的长连接监控（WebSocket），将长时间的音视频渲染和拼接待办进度动态、稳定地呈现给用户。

### 5. 前端开发与 UI/UX 设计师 (shaddy's girlfriend)

- **抽象职责**：产品原型设计规划、用户体验研究、视觉风格定义规范、基础前端呈现与界面打磨。
- **具体项目职责**：
  - 依据《中文user.md》绘制和梳理高保真产品原型与操作流程图。
  - 完善从控制台、大盘数据面板到视频详情播放器底座的 UI 可视化设计，确保 AI 科技感和生产力工具的干练风格。
  - 协助前端主程完成部分切图、编写 CSS 样式组件、各种平滑转场动画，并做多设备终端的响应式重构体验适配。

## 8. 团队协作边界与数据流转规范 (防内耗机制)

为了保证 5 人战队的自洽性与零代码冲突，系统的职责开发制定了以下严格的边界：

### 8.1 核心边界定义

- **AI 引擎核心（架构师）与 后端开发 的边界**
  - **解耦原则**：后端只负责“业务与数据 CRUD”，提供内网 API 和任务队列；AI 引擎不写任何用户逻辑，只负责“监听队列 -> 处理算力和API黑盒 -> 产出视频 -> 回调写入”。
  - **数据流向**：后端截留所有直连用户的请求，进行积分扣除校验与敏感词拦截后，才把干净的任务推入 AI 异步流引擎（如 Redis/Celery 队列）。
- **数据源层（爬虫）与 持久化存储层（后端）的边界**
  - **解耦原则**：爬虫工程师禁止越权绕开业务层，直连 OSS文件存储 或 生产环境 MySQL 库。
  - **数据流向**：爬虫爬取并进行清洗后的格式化数据（源短视频、图片、大盘JSON），统一通过调用“后端内部数据网关 API”推送，由后端负责鉴权后自动落盘。
- **前端交互（客户端专家）与 前端呈现（UI/UX）的边界**
  - **解耦原则**：严格执行【上游展示组件】与【下游业务组件】的代码分离模式。
  - **开发流向**：UI/UX 工程师交付无逻辑但样式完美的视觉组件（Dumb Components，聚焦 HTML/CSS/动效）；前端交互工程师负责接入全局状态（Redux/Zustand）与 WebSocket 长连接，为其注入真实生命流。避免两人在同一文件发生业务流与样式的代码冲突。

### 8.2 典型工作流闭环演练（数据单向流验证）

以核心业务即**“获取外部社媒热点并一键转化自动生成视频”**为例的团队协作运转流：

1. **[爬虫工程师]**：于后台定时抓取各类短视频热榜并捕获原素材，经过格式化后发出请求。
2. **[后端工程师]**：接收爬虫系统的载荷，自动落库至数据库与 OSS 形成“实时待选素材池”。
3. **[前端主程 + UI/UX]**：在 Web 端获取这些最新热门素材流，配合设计好的高颜展现样式呈现给终端用户。
4. **[用户控制端]**：用户二次调整编排提交生成指令。后端接口接收其任务进行**计费与配额校验**并推入任务网关。
5. **[架构师与AI引擎]**：微服务节点捕获任务指令，激活 `AssetBuilder` 与底层大模型调用（Video Gen V2、SD 等），进行素材二次封装打磨与拼接渲染。
6. **[后端工程师]**：接收 AI 引擎回传的回调结果与高清视频 URL，更新数据库记录状态。
7. **[后端向前端机制]**：后端凭借事先握手的 WebSocket / SSE 通道将消息广播至前端，完成交互链路的一键闭环。

在这种“**源数据 -> 业务网关 -> 任务派发 -> AI推演 -> 视图呈现**”的单向环状数据流运转中，团队不仅明确了谁修Bug、而且保障了极高的并行开发效率。
